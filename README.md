# Employee Time Tracker

Testing PHP application. lol

To run:

1. Import database: `mysql -u root -p < database.sql`
2. Start server: `php -S localhost:8000`
3. Visit: `http://localhost:8000/`
4. Login with: `demo@example.com` / `demo123`
